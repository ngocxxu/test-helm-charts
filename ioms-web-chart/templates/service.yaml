apiVersion: v1
kind: Service
metadata:
  name: {{ include "ioms-web-chart.fullname" . }}
  labels:
    {{- include "ioms-web-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ioms-web-chart.selectorLabels" . | nindent 4 }}
